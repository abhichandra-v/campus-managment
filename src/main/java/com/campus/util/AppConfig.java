package com.campus.util;

/** Small app-wide constants that don't warrant an external config file yet. */
public final class AppConfig {

    /** The semester students see by default when browsing courses to enroll in. */
    public static final String CURRENT_SEMESTER = "FALL2024";

    private AppConfig() {
    }
}
